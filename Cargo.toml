[package]
name = "g-server"
version = "0.0.0"
edition = "2024"
authors = ["Muhammad Fathir Irhas <fathir.irhas@gmail.com>"]
description = "..."
license = "MIT"
repository = "https://github.com/mfirhas/g-server"
documentation = "https://docs.rs/g-server"
keywords = ["http", "sse", "websocket", "mcp", "server"]
categories = ["rust-patterns"]
rust-version = "1.86.0"
exclude = [
  "**/*_test.rs",
  "tests/**",
  "benches/**",
  "examples/**",
  ".github/**",
  ".gitignore",
  "Makefile",
  "AUDIT.md",
  "AGENTS.md",
]

[package.metadata.docs.rs]
all-features = true

[[test]]
name = "test"
path = "tests/test.rs"

[[bench]]
name = "bench"
harness = false

[dependencies]

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

